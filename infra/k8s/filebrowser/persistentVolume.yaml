apiVersion: v1
kind: PersistentVolume
metadata:
  name: filebrowser-pv
spec:
  capacity:
    storage: 10Gi  # ← ここを10Giに修正！
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /srv/share
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""  # 空でOK
  volumeMode: Filesystem

