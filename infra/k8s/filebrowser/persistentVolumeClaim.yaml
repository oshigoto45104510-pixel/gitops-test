apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebrowser-pvc
  namespace: filebrowser
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""         # ← 明示的にストレージクラスを使わない
  volumeName: filebrowser-pv   # ← このPVを明示的に使う

