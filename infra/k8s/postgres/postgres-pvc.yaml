apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-my-postgres-postgresql-0
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
  volumeName: postgres-pv

